[project]
name = "simulator"
version = "0.1.0"
description = "UnitLab simulator service of esp32 modules"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gmqtt",
    "asyncio",
    "pyyaml",
]