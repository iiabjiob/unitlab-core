# UnitLab local NTP serving for peripheral devices on the AP subnet.
# This keeps the RPi as a single time base for ESP32 modules even when
# no upstream NTP source is currently synchronised.

allow 10.42.0.0/24
local stratum 10